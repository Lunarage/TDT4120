class Character:
    def __init__(self, char, freq):
        self.freq = freq
        self.char = char


def huffman(characters):
    for _ in range(characters.len - 1):
        

characters = [
    Character("A", 2),
    Character("B", 2),
    Character("C", 5),
    Character("D", 10),
]
